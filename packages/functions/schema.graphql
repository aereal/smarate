type Query {
  visitor: User
}

type Mutation {
  setPreference(defaultFighterID: Int): Boolean!
  recordResult(
    myFighterID: Int!,
    rivalFighterID: Int!,
    won: Boolean!
  ): Boolean!
}

type User {
  id: String!
  preference: UserPreference!
}

type UserPreference {
  defaultFighterID: Int!
}
